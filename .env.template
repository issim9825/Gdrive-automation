# Environment Configuration Template, fill in the values and rename to .env
# Canvas Configuration
CANVAS_TOKEN=your_canvas_api_token
CANVAS_URL=https://youruniversity.instructure.com/
COURSE_ID=id_of_your_course

# Local Storage
# Where files are saved on your computer before uploading
LOCAL_DIR=./canvas_docs

# Rclone / Google Drive Configuration
# REMOTE_NAME is what you called your drive in 'rclone config'
# REMOTE_PATH is the folder inside your Google Drive
REMOTE_NAME=rclone_remote_name
REMOTE_PATH=path/inside/your_drive